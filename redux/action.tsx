import axios from 'axios';
import { GET_ACCOUNT, ERROR } from './constants';
import RNFS from 'react-native-fs';

//create new account
export function getAccount() {
  return async (dispatch) => {
    try{
    }
    catch (error) {
      console.log(error);
    }
  };
}